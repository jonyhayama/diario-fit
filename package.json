{
  "name": "diario-fit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "yarn --cwd frontend --production=false install",
    "build": "yarn --cwd frontend build",
    "serve": "yarn --cwd frontend serve",
    "test:unit": "yarn --cwd frontend test:unit",
    "lint": "yarn --cwd frontend lint"
  }
}